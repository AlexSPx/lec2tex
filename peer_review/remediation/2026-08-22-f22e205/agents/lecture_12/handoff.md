# Lecture 12 handoff

Completed the nine assigned resolutions (ADJ-064--ADJ-071 and ADJ-083) in
`lectures/bodies/lecture_12.tex`. The body now states iid sampling as a model
assumption; defines likelihood using a pmf/density for fixed data; preserves
both Uniform support indicators while disclosing the closed-endpoint corrective
convention; qualifies the positive-variance normal MLE and its boundary failure;
states MoM existence/selection and inverse conditions; marks the formal SLLN and
Cramer--Rao content as authored supplements; states Cramer--Rao regularity and
Uniform nonregularity; repairs Uniform epsilon and sample-size boundaries; and
documents the local sample-moment notation convention.

Only the lecture body and this agent audit directory were changed. The accepted
retry fidelity records were used for SRC-COLL-001/002/003; invalidated fidelity
records were ignored. `git diff --check` passed. No global build was run. Shared
files (frontmatter, formulas, and other lectures) remain for the coordinator's
central pass.
