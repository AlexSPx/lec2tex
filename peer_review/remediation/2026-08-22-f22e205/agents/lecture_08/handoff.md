# Lecture 08 handoff

Resolved the Lecture 08 body findings for conditioning domains, conditional-
expectation scope, CDF differentiation, one-dimensional transformation
regularity/support, exponential-minimum support, moment existence, and
pedagogical provenance. The body now uses positive-probability atoms,
integrability/L2 hypotheses, a regular-version/a.e. caveat for null point
conditioning, an explicit pushforward interpretation for duplicate conditional
means, an a.e. CDF derivative, and a C1 change-of-variables theorem with a
zero-off-image branch. The medical example and exercises are labeled as
editor-created additions.

ADJ-011, ADJ-033, ADJ-034, and ADJ-037 are marked partially resolved because
their shared `formulas.tex` occurrences were intentionally left for the
coordinator's central pass. No generated files, formulas, frontmatter,
preamble, PDFs, or other lectures were edited. Scoped validation passed:
`git diff --check`, audit JSONL parsing, TeX environment/brace checks. No
book-wide build was run.
