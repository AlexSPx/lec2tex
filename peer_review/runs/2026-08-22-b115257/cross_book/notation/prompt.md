# Cross-book notation, convention, and parameterization audit

Run: `2026-08-22-b115257`  
Scope: `lectures/bodies/lecture_01.tex` through `lecture_15.tex`,
`lectures/bodies/formulas.tex`, `lectures/bodies/frontmatter.tex`, and
`lectures/preamble.tex`; completed math and completed fidelity outputs in this
run were used only as corroborating review evidence. The audit did not read
`docs/REMEDIATION.md` or Git history, and it did not edit book sources.

Audit dimensions: `\E`, `\Var`, `\ind`, `\given`, strict-CDF convention,
distribution support and parameterization, Gamma rate/scale language, Normal/
chi-square/Student notation, independence and measurability conditions,
convergence language, regression/statistics symbols, repeated hypotheses, and
formula-appendix agreement.

Finding standard: record a finding only where the source text is internally
wrong, conflicts with the shared convention, or loses a condition which a
different scoped source establishes as necessary. Line references are source
line numbers in the current working tree. `P1` materially permits an invalid
calculation or undefined expression; `P2` is a substantive rigor/notation
defect; confidence is evidence confidence, not an estimate of importance.
