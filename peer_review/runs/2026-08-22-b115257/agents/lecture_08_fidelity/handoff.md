# Lecture 08 fidelity handoff

Independent inventory: 14 entries (10 preserved, 3 added, 1 mixed).

Blind adjudication: all 8 IDs appear exactly once in `verdicts.jsonl` (6
`confirmed_book_error`, 2 `fidelity_omission_or_strengthening`). Four
independent provenance gaps appear in `new_findings.jsonl`. The prior
`new_fidelity_findings.jsonl` is intentionally preserved for compatibility.

Targeted OCR used: conditional expectation `board_008`; density/CDF
`board_055`, `board_056`; transformation `board_060`; supporting frames
`board_012`, `board_015`, `board_017`-`board_019`, `board_061`-`board_063`.
