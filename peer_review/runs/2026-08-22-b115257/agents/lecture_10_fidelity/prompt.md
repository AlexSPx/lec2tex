# Lecture 10 fidelity audit

Compare `lectures/bodies/lecture_10.tex` with the complete timestamped transcript in `run/lecture_10/audio/transcript.json`. Persist a complete independent timestamped source map before opening `agents/lecture_10_math/findings.jsonl`. Adjudicate every blind finding using the fidelity-rubric taxonomy; use targeted board/OCR checks for convergence, inequalities, and LLN claims; identify independent omissions/strengthenings; make no book edits; and validate machine-readable artifacts.
