# Lecture 13 fidelity verification

Perform Stage A before reading the blind findings: compare the complete audio
transcript and board/OCR evidence to `lectures/bodies/lecture_13.tex`, record
timestamped fidelity coverage, then adjudicate every blind finding and seek
source omissions or strengthenings.  Do not edit the lecture body.

Canonical artifacts are `state.json`, `events.jsonl`,
`fidelity_inventory.jsonl`, `verdicts.jsonl`, `new_findings.jsonl`, and this
handoff.  `independent_source_map.jsonl` and `stage_a_checkpoints.json` are
preserved pre-contract artifacts.
