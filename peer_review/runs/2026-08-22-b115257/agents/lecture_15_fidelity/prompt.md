# Lecture 15 fidelity audit

Compare `lectures/bodies/lecture_15.tex` against the complete timestamped
transcript in `run/lecture_15/audio/transcript.json`.  Stage A must be
persisted before opening blind mathematical findings.  Stage B must adjudicate
every blind ID using the rubric verdict taxonomy, identify independent fidelity
omissions/strengthenings, consult board/OCR for path-count disputes, make no
book edits, and validate all audit artifacts.

Canonical correction (2026-08-22): retain the original `stage_a_map.jsonl` and
`findings.jsonl` as historical work products; use `fidelity_inventory.jsonl`,
`verdicts.jsonl`, and `new_findings.jsonl` as the canonical machine-readable
deliverables. Required counts are 13 inventory records, exactly 4 blind-ID
verdict records, and 6 independent findings.
